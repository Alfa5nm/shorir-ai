# Security Policy

Report security or privacy issues privately to the repository owner instead of opening a public issue.

Do not include credentials, personal health details, camera frames, meal images, access tokens, or production
URLs in reports. Rotate any credential immediately if it is accidentally exposed.

The public repository intentionally excludes local environment files, original submission documents, Supabase
temporary state, tunnel artifacts, and generated build output.
